

#### 1) What is the difference between var, let, and const?
ans:var is a function scoped variable, its function scoped,its gets hoisted in the top of its scope & gives undefined!let is a variable that can be re-declared,,accessing before declaration can give rteference error ,,,const is a variable that can not be re-assigned!

#### 2) What is the difference between map(), forEach(), and filter()? 
ans:map() is a method that takes an array and do something with each item & returns back an new array of those!forEach() takes the items do something but dont give back a anew array or returns!filter() filters the items that meets specific condition  & returns a new arary !!!

#### 3) What are arrow functions in ES6?
ans:arrow functions are shorter way to to write functions,,,if an arrow function is one expression it automatically returns the result,,,its easier & shorter in syntax!!!

#### 4) How does destructuring assignment work in ES6?
ans:its extract values from array or objects & assign them to variables in a shorter , cleaner way!!!

#### 5) Explain template literals in ES6. How are they different from string concatenation?
ans:template literals are strings wrapped in backticks ` instead of quatas "" ,,,whats makes them different from string concatenation is that in the template literal it allows to usse variables directly inside the string using ${....}...its also used for dynamic ui!!!!



---
